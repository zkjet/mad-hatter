{
  "$jsonSchema": {
    "bsonType": "object",
    "required": [
      "_id"
    ],
    "properties": {
      "_id": {
        "bsonType": "objectId",
        "description": "Only one object in this collection"
      },
      "channels": {
        "bsonType": "array",
        "description": "Contains channel shortlist",
      }
    }
  }
}